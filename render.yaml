services:
  - type: web
    name: ai-assistant-tamil
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
